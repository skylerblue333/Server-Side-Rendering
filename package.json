{
  "name": "sky-ssr-core",
  "version": "0.1.0",
  "private": true,
  "description": "Dependency-free TypeScript server-side rendering core for bounded HTML pages.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/tests/*.test.js",
    "start": "node dist/src/server.js",
    "check": "npm run build && npm test"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
